/**
 * Look into SipHash and Quadratic Probing open addressing (what rust implements for hashmap)
 *
 */

pub trait Map {
    // polynomial rolling algorithm
    //
    fn get();
    fn set();
    fn contains();

    fn hash();
}
